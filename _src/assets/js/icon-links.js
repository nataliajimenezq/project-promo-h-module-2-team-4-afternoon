
/*

const links = document.querySelectorAll('.card__list--icon');

const icons = {

}


for(let input of inputs){


function showIconMail() {
    for(i=0; i<inputs.length; i++)
    input[0].value.length > 0  {
        emailIcon.classList.remove('hidden');


    (input[1].value.length > 0) {
        phoneIcon.classList.remove('hidden');
    }
    if (input[2].value.length > 0) {
        linkedinIcon.classList.remove('hidden');
    }
    if (input[3]e.length > 0) {
        gitIcon.classList.remove('hidden');
    }
}



}


function addLinksIcons() {
    emailLink.innerHTMl = `<a href=${input[0].value}class="card__list--icon-mail"`;
}

input.addEventListener('keyUp', addLinksIcons());*/